# TAS-TTAS-and-Backoff-locks
implementation of TAS, TTAS and Backoff locks in c++
compile the code using "g++ filename -lpthread" 
run it using "./a.out".
it prints the worst case waiting time and average waiting time.
